# Add Channel Refresh Button

## Task
Add a force refresh button for the channel cache on the Rules page.

## Changes
- Added refresh button next to "Add Rule" section title in `Rules.tsx`
- Button calls `fetchChannels(true)` to bypass 5-min cache TTL
- Shows loading state ("⏳ Refreshing…") while fetching

## Result
- [x] Refresh button added to Rules page
- [x] Button disabled during refresh to prevent duplicate requests
- [x] Channel list updates immediately after refresh
