# 全站中文化 + 消息内容预览

## 需求
- 前端所有页面显示为中文
- 消息记录需要存储并展示文本预览内容（标题/正文摘要）
- 修复 /api/messages 斜杠重定向导致的 401 跳转问题

## 变更列表

### 后端
- [x] `MessageLog` 模型新增 `text_preview: String(200)` 字段
- [x] `MessageResponse` schema 新增 `text_preview` 字段
- [x] `forwarder.py` 转发时截取 `message.text[:200]` 写入 text_preview
- [x] album 转发同样保存首条消息的 text_preview

### 前端中文化
- [x] 侧边栏：仪表盘 / 转发规则 / 消息记录 / 系统日志 / 账户
- [x] 仪表盘：连接状态、活跃规则、今日转发/失败、实时事件
- [x] 转发规则：添加规则、刷新频道、同步状态、操作按钮
- [x] 消息记录：筛选器、表头、分页、状态/类型中文标签
- [x] 系统日志：标题、筛选按钮
- [x] 账户：手机号/验证码/两步验证步骤、已认证信息
- [x] 登录页：placeholder 中文化

### 消息内容预览
- [x] 消息列表新增「内容」列，显示 text_preview（最多 40 字符 + 省略号）
- [x] 内容类型中文映射：文本/图片/视频/文件/音频/语音/贴纸/动图/相册
- [x] 状态中文映射：成功/失败/等待中
- [x] 移除 Source Msg / Target Msg 列，精简表格

### Bug 修复
- [x] `fetchMessages` 路径 `/api/messages` → `/api/messages/` 修复 307 重定向丢失 Authorization header
